import 'person.dart';

// Employee (name,year,salary) using constructor..

void main() {
  // create an object
  Person p1 = Person();
  p1.name = 'Ali';
  print(p1.getName());
  print(p1.getAge());
  print(p1.age);
}
